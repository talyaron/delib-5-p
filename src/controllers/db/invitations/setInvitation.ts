interface CreateInvitationProps {
    pathname: string;
    statementId: string;
}
export async function setInvitationToDB({ pathname, statementId }: CreateInvitationProps) {
    try {
        
    } catch (error) {
        console.error(error);
    }
}